#include<stdio.h>
int main()
{
    int n,i,j,k;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);
    for(i=0;i<n;i++)
        for(j=1;j<a[i]/2;j++)
            for(k=1;k<a[i]/4;k++)
            {
                if(m==2*j+4*k)
                    printf("")
            }
       return 0;
}
